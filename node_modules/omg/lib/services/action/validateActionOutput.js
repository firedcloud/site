"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = validateActionOutput;

var _omgValidate = require("omg-validate");

var _errors = require("../../errors");

function validateActionOutput({
  action,
  actionName,
  output
}) {
  const errors = (0, _omgValidate.validateArgout)(action.output, output);

  if (errors.length) {
    throw new _errors.CLIError(`Output validation failed for Action#${actionName}\n${errors.map(item => `  - ${item}`)}`);
  }
}