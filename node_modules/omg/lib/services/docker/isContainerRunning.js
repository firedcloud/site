"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isContainerRunning;

var _common = require("./common");

async function isContainerRunning(container) {
  const [containerInfo] = await _common.dockerode.listContainers({
    filters: {
      id: [container]
    }
  });

  if (!containerInfo || containerInfo.State !== 'running') {
    return false;
  }

  return true;
}