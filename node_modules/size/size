#!/usr/bin/env node

/**
 * Module dependencies.
 */

var bytes = require('bytes');

var n = 0;

process.stdin.on('data', function(chunk){
  n += chunk.length;
}).on('end', function(){
  console.log(bytes(n));
}).resume();
