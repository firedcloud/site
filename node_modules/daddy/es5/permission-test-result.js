"use strict";

var _interopRequire = function (obj) { return obj && obj.__esModule ? obj["default"] : obj; };

/**
 * A convenience wrapper for test results
 * 
 * @param {Boolean} granted
 * @param {any}     reason
 */
module.exports = PermissionTestResult;
var check = _interopRequire(require("check-types"));

var hastobe = check.assert;function PermissionTestResult(granted, reason) {
  hastobe.boolean(granted);

  this.granted = granted;
  this.reason = reason;

  return this;
}